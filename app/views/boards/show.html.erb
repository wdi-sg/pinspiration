<h2><%=@board.name%></h2>

<%= link_to image_tag("addnew.svg", :class => "add-new-pin" ), new_board_pin_url(@board) %>

<div></div>

<% @pins.each do |pin| %>
	<%= link_to image_tag(pin.image, class:"img-fluid img-thumbnail board-all-pins"), board_pin_path(@board, pin) %>
<% end %>