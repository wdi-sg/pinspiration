<%= form_with scope: :comment, url: comments_path, local: true do |form| %>
<img src="<%= @pin.url %>">
  <p>
    <%= form.label :comment %><br>
    <%= form.text_area :body %>
  </p>

  <p>
    <%= form.submit %>
  </p>
<% end %>
<%= link_to 'Back to pin', @pin %>