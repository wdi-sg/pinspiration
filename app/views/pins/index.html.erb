<h1>WELCOME TO PINSPIRATION!</h1>

<table>

  <% @pins.each do |pin| %>
    <tr>
      <td><%= pin.title %></td>
      <td><%= image_tag pin.image_url,:size => "150x150" %></td>
      <td><%= link_to 'Show', pin_path(pin) %></td>
    </tr>
  <% end %>
</table>
