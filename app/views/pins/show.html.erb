<p>
  <strong>Pin:</strong>
  <%= @pin.title %>
</p>

<p>
<strong>Image:</strong>
<img src="<%= image_url @pin.image %>"/>

</p>

<% if user_signed_in? %>
<%= link_to 'Edit', edit_pin_path(@pin) %>
<% end %>
<%= link_to 'Back', root_path %>