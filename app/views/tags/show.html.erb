<p>All pins tagged with: <%= @tag.name %></p>

<% @pins.each do |pin| %>
<a href="/pins/<%= pin.id%>"><%= image_tag(url_for(pin.url), {:class=>"pin-img"}) %></a>
<% end %>

<p><%= link_to "Home", root_path %></p>